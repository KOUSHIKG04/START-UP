# Shared contracts scaffold

The TypeScript files are intentionally empty. They are reserved for shared domain
contracts and input validation when backend integration begins. No runtime exports
or dependencies are available yet. Database-generated types belong in data-access.
